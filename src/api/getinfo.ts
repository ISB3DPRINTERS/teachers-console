//import * as fs from 'fs'

const getcost = (grade:number) => {
    // ask for cost per grade
    return ("not developed yet"+grade);
  }
const getkey = (grade:number) => {
    // ask for key per grade
    return ("not developed yet"+grade)
}

export default {getcost, getkey}